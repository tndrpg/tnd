defmodule Tnd.Games do
  @moduledoc """
  The Games context.
  """

  # def list_games(user_id), do: nil
  # def create_game(game_attrs \\ %{}), do: nil
  # def get_game(game_id), do: nil
  # def join_game(game_id, user_id), do: nil
  # def leave_game(game_membership_id), do: nil

  # def send_message(game_id, message_attrs \\ %{}), do: nil
  # def get_chunk_of_messages(game_id, before_message_id, chunk_size \\ 20), do: nil
end
