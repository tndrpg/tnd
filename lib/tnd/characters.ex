defmodule Tnd.Characters do
  @moduledoc """
  The Characters context.
  """

  alias Tnd.Characters.{Armor, ArmorAttachment, Character, Gear, Skill, WeaponAttachment, Weapon}

  # def list_characters(compendium_id), do: nil
  # def create_character(character_id, attrs \\ %{}), do: nil
  # def get_character(character_id), do: nil
  # def edit_character(character_id, attrs \\ %{}), do: nil
  # def delete_character(character_id), do: nil

  # def add_weapon_to_character(character_id, weapon_attrs \\ %{}), do: nil
  # def edit_character_weapon(character_weapon_id, character_weapon_attrs \\ %{}), do: nil
  # def remove_weapon_from_character(character_weapon_id), do: nil
  # def add_weapon_attachment_to_character_weapon(character_weapon_id, weapon_attachemnt_attrs \\ %{}), do: nil
  # def remove_weapon_attachment_from_character_weapon(character_weapon_attachment_id)

  # def add_armor_to_character(character_id, armor_attrs \\ %{}), do: nil
  # def edit_character_armor(character_armor_id, character_armor_attrs \\ %{}), do: nil
  # def remove_armor_from_character(character_armor_id), do: nil
  # def add_armor_attachment_to_character_armor(character_armor_id, armor_attachemnt_attrs \\ %{}), do: nil
  # def remove_armor_attachment_from_character_armor(character_armor_attachment_id)

  # def add_skill_to_character(character_id, skill_attrs \\ %{}), do: nil
  # def set_character_skill_rank(character_skill_id, rank), do: nil
  # def remove_skill_from_character(character_skill_id), do: nil
  # def toggle_character_career_skill(character_skill_id), do :nil
end
